# إعداد مَقْلَمَة على Supabase الجديد

1. افتح Supabase > SQL Editor.
2. أنشئ New query.
3. انسخ محتوى `supabase.sql` كاملًا ثم Run.
4. من Authentication > Users أنشئ أول حساب Admin باستخدام بريد إلكتروني حقيقي وكلمة مرور قوية.
5. بعد إنشاء المستخدم، نفّذ في SQL Editor (استبدل البريد):

```sql
update public.profiles
set role = 'admin'
where email = 'YOUR_ADMIN_EMAIL';

update public.user_roles
set role = 'admin'
where user_id = (select id from public.profiles where email = 'YOUR_ADMIN_EMAIL');
```

6. لا تضع Service Role / Secret Key داخل `env.js` أو المتصفح.
7. شغّل المشروع واستخدم اسم المستخدم الموجود في `profiles.username` وكلمة المرور التي أنشأتها في Auth.

ملاحظة: النسخة الحالية لا تنشئ Admin تلقائيًا من المتصفح. هذا مقصود لأمان الصلاحيات.

## تحديث: نظام الاختبارات بمؤقّت (تسليم تلقائي عند انتهاء الوقت)

هذا التحديث يضيف فقط إلى نظام الاختبارات الموجود (لا يغيّر أي جدول أو صفحة أخرى):

- مدة كل اختبار (بالدقائق) يحدّدها المدير عند إنشاء/تعديل الاختبار.
- Countdown يظهر للطالب فور بدء الاختبار، ويُحسب اعتمادًا على وقت السيرفر لا المتصفح.
- عند انتهاء الوقت يتم التسليم والتصحيح تلقائيًا وإغلاق المحاولة (لا يمكن إعادة الاختبار بعدها).
- وقت البداية والنهاية والتحقق من عدم تكرار المحاولة كلها تتم عبر Database Functions (`start_quiz_attempt` و`submit_quiz_attempt`)، وليس من المتصفح.
- صفحة نتائج كل اختبار للمدير أصبحت تعرض أيضًا تاريخ ووقت التسليم وحالة المحاولة (تسليم عادي / تسليم تلقائي بسبب انتهاء الوقت)، بالإضافة إلى زر **"طباعة النتائج / PDF"** لتصدير تقرير منسّق بالعربية وRTL لهذا الاختبار فقط، جاهز للطباعة أو الحفظ كـPDF.

**خطوة التطبيق:** أعد تشغيل ملف `supabase.sql` كاملًا في SQL Editor مرة واحدة (آمن على قاعدة بيانات موجودة — كل الأوامر فيه `if not exists` / `create or replace`). هذا سيضيف الأعمدة والجداول والدوال الجديدة دون التأثير على أي بيانات موجودة. الاختبارات القديمة ستحصل تلقائيًا على مدة افتراضية (10 دقائق) يمكن للمدير تعديلها من صفحة "إدارة الاختبارات".

## تحديث: جلسة واحدة فقط لكل حساب طالب (Single Device Session)

يضيف فقط إلى المخطّط الموجود (لا يحذف أو يعدّل أي بيانات):

- أعمدة جديدة في `profiles`: `active_session_id`, `active_device_label`, `session_updated_at`.
- دوال جديدة: `claim_device_session()`, `release_device_session()`, `is_my_session_active()`, `session_ok()`, `request_device_id()`.
- Trigger جديد على `profiles` يمنع أي طالب من تعديل عمود الجلسة النشطة مباشرة.
- سياسات RLS محدَّثة لـ: `lessons`, `live_streams`, `chat_messages`, ودالتَي `_lesson_file_access()` و`_quiz_access()` (وبالتالي الاختبارات كلها) — كل واحدة منها أصبحت تتحقق أيضاً من `session_ok()`.

**خطوات التطبيق:**

1. أعد تشغيل `supabase.sql` كاملًا في SQL Editor مرة واحدة (نفس الملف المحدَّث، آمن على مشروع قائم).
2. انشر Edge Function الجديدة:
   ```bash
   supabase functions deploy session-guard
   ```
   لا تحتاج أي أسرار إضافية — تستخدم نفس `SUPABASE_URL` / `SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_ROLE_KEY` التلقائية التي تستخدمها `admin-users` بالفعل. إن لم تكن `admin-users` منشورة من قبل، انشرها أيضاً بنفس الطريقة:
   ```bash
   supabase functions deploy admin-users
   ```
3. (اختياري، تشديد إضافي) قلّل مدة صلاحية الـ Access Token من **Authentication → Settings** حتى يُستكمَل إبطال الجهاز القديم بسرعة أكبر حتى لو تجاهل الجهاز نفسه إشعار تسجيل الخروج الفوري.

لا حاجة لأي تعديل يدوي على بيانات الطلاب الحاليين — أول تسجيل دخول لكل طالب بعد هذا التحديث يفعّل نظام الجهاز الواحد لحسابه تلقائياً.
